create database empregados;
use empregados;
create table departamento(
	cod_depto int not auto_increment,    
    nome varchar (30) nor null,
    constraent pk_departamento primary key (cod_depto)
);
